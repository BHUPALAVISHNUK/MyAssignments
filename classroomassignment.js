// const browserversion = "edge"
// function getbrowserversion(){
//     if (browser === "chrome") {
//         var browser = "chrome";      
//     }
//     console.log(browserversion);
// }
// getbrowserversion();

const browserversion = 'chrome'
function getBrowserVersion(){
    if (browserversion === "chrome") {
        let browserversion = "firefore"
        
    }
    console.log(browserversion)
}
getBrowserVersion();